Функция КлючЗаписиЭлементаСписка(Форма, ИмяСписка) Экспорт
	
	Результат = Новый Структура;
	ПустойРезультат = Неопределено;
	
	#Область Предусловия
	
	Если (Не ЗначениеЗаполнено(ИмяСписка)) Тогда
		Возврат ПустойРезультат;
	КонецЕсли;
	
	#КонецОбласти
	
	Результат = Форма.Элементы[ИмяСписка].ТекущаяСтрока;
	
	Возврат Результат; 
	
КонецФункции